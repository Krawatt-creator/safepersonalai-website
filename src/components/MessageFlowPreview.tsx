"use client";

import { useState } from "react";

export default function MessageFlowPreview() {
  const [approved, setApproved] = useState(false);
  return <div className="relative overflow-hidden rounded-2xl border border-border-strong bg-bg-card p-5 shadow-[0_30px_90px_-30px_rgba(0,0,0,.65)]" aria-label="Illustrative iMessage conversation"><div className="mb-4 flex items-center gap-2"><span className="grid h-7 w-7 place-items-center rounded-full bg-green text-xs font-bold text-white">✓</span><div><p className="text-xs font-semibold text-text">Messages · SafePersonalAI</p><p className="text-[10px] text-text-tertiary">Illustrative preview · synthetic example</p></div><span className="ml-auto rounded-full border border-green/30 bg-green/10 px-2 py-1 text-[9px] font-medium text-green">Local demo</span></div><div className="message-flow space-y-2 text-xs"><div className="message-bubble message-in">Remind me to renew my passport on Friday.</div><div className="message-bubble message-out">Got it — I prepared a reminder for Friday at 09:00.</div><div className="message-bubble message-card"><span className="text-text-tertiary">Pending Action</span><strong className="mt-1 block text-text">Passport renewal</strong><span className="mt-1 block text-[10px] text-text-tertiary">Nothing is sent until you approve.</span>{approved ? <div className="mt-3 flex items-center justify-between gap-3" aria-live="polite"><span className="text-[10px] font-semibold text-green">✓ Saved to your local to-dos</span><button type="button" onClick={() => setApproved(false)} className="text-[10px] font-medium text-text-tertiary underline underline-offset-2 hover:text-text focus-visible:outline-2 focus-visible:outline-green focus-visible:outline-offset-2">Replay</button></div> : <button type="button" onClick={() => setApproved(true)} className="site-cta-primary mt-3 min-h-0 px-3 py-1.5 text-[10px]" aria-label="Approve illustrative passport reminder">Approve reminder</button>}</div></div><p className="mt-4 text-[10px] text-text-tertiary">Synthetic conversation · clicking only changes this preview.</p></div>;
}
